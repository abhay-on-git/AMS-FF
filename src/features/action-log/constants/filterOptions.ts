import type { EntityType, EventType } from '../types'

export const ENTITY_TYPES: EntityType[] = [
  'Assets',
  'Users',
  'Categories',
  'Locations',
  'Reports',
]

export const EVENT_TYPES: EventType[] = ['Create', 'Update', 'Delete']
